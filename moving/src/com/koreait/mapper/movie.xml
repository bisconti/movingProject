<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Movie">
	<select id="mostFast">
		select md.moviephoto from movie m join moviedata md 
		on m.movienum = md.movienum where m.movienum = 
		(select movienum from movie order by movierelease desc limit ${i},1);
	</select>
	<select id="watched">
		select md.moviephoto from moviedata md join watched w
		on md.movienum = w.movienum where md.movienum =
		(select movienum from watched order by time desc limit ${i},1)
	</select>
	<select id="mostView">
		select md.moviephoto from moviedata md join movie m
		on m.movienum = md.movienum where md.movienum =
		(select movienum from movie order by view_cnt desc limit ${i},1)
	</select>
	<select id="mostLike">
		(select count(movienum) from movie_like order by )
	</select>
	
</mapper>